apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next-form-helm.fullname" . }}-test-connection"
  labels:
    {{- include "next-form-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next-form-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
