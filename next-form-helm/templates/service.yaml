apiVersion: v1
kind: Service
metadata:
  name: {{ include "next-form-helm.fullname" . }}
  labels:
    {{- include "next-form-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "next-form-helm.selectorLabels" . | nindent 4 }}
